# NoSleep

NoSleep 是一个简单的 Android 网页常亮工具。它可以在内置 WebView 中打开用户输入的网页，并在指定时间内保持屏幕常亮，适合需要长时间查看网页看板、工作页面或本地服务页面的场景。

## 主要功能

- 自定义输入访问 URL。
- 在 App 内置 WebView 中打开网页。
- 支持常亮时长：30 分钟、1 小时、3 小时、6 小时、8 小时、12 小时。
- 到达常亮时长后自动撤销常亮控制，恢复系统息屏策略。
- 支持保存常用 URL 预设。
- 工作页启用安全窗口，减少系统截图和录屏风险。

## 使用方法

1. 安装 APK 并打开 App。
2. 在 `访问URL` 中输入你有权访问的网页地址，例如 `https://your-domain.example/path`。
3. 勾选 `使用确认`。
4. 选择需要的 `工作页常亮时长`。
5. 点击 `打开网页`。

如需下次快速打开同一地址，可以填写 `预设名称（可选）`，然后点击 `保存预设`。保存后的预设会显示在首页，点击 `应用` 可快速填回 URL 和常亮时长。

## 隐私与数据

网页地址与访问内容                                                                                                                                              
                                                                                                                                                                  
  本应用提供本机网页打开与屏幕常亮辅助功能。用户可自行输入网页地址，并在设备本机的 WebView 中打开相关网页。                                                       
                                                                                                                                                                  
  本应用不会主动上传、收集、存储或分析用户输入的网页地址、网页内容、账号信息、页面操作记录或访问行为。网页的加载、登录、鉴权、Cookie、缓存及相关数据由 Android    
  系统 WebView 和目标网页服务按照其自身机制处理。
                                                                                                                                                                  
  如用户主动使用“保存预设”功能，本应用会将用户输入的网页地址、预设名称及常亮时长保存在当前设备本机，用于下次快速填充和打开。上述数据不会被本应用上传至服务器，也不
  会与第三方共享。用户可在应用内删除已保存的预设。
                                                                                                                                                                  
  用户应确保其输入和访问的网页地址合法、合规，并已取得相应访问权限。本应用仅根据用户输入在本机打开网页，不提供任何网页内容、账号授权、身份认证或权限绕过能力。因用
  户输入的网址、访问行为及展示内容产生的责任，由用户自行承担。
                                                                                                                                                                  
  如果你的 App 没有账号系统、没有服务器、没有埋点，这版基本匹配当前产品形态。                                                                                     
  建议同时在隐私政策里单独加一句：
                                                                                                                                                                  
  本应用不提供用户注册、账号登录、云同步、广告投放、行为分析或远程数据上传功能。

## 构建

推荐使用 Android Studio 自带 JDK：

```bash
JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" \
GRADLE_USER_HOME="$PWD/.gradle-home" \
./gradlew :app:assembleDebug --no-daemon
```

生成的 Debug APK 位于：

```text
app/build/outputs/apk/debug/app-debug.apk
```
## 注意事项
- 长时间常亮会增加耗电和发热，请按实际需要选择常亮时长。
- 使用者应确认自己有权访问输入的 URL，并自行承担访问内容相关责任。
- Android 厂商系统可能会对后台、WebView 或电源策略有差异，建议以真机测试结果为准。
- 本项目已基于 AICoding 完成开发，现已停止维护。
